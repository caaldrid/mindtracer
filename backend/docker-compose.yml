services:
  postgres:
    image: postgres:18.3-trixie
    container_name: postgres
    ports:
      - 6500:5432
    env_file:
      - ./app.env
    volumes:
      - postgres:/var/lib/postgresql/
volumes:
  postgres:
